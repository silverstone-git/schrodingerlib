# schrodingerlib
Physical Functions, implemented in python

## How to build numerical_analysis and run it

```
cd numerical_analysis/
make -C build && ./build/runme
```


## Main project run

```
export VCPKG_ROOT=/path/to/vcpkg
cmake --preset default
cmake --build build
```
